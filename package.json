{
  "name": "aplifyr",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"dotnet run --project backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}